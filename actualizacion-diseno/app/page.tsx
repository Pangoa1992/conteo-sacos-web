import { Dashboard } from "@/components/Dashboard";

/**
 * Página principal: dashboard de conteo en tiempo real.
 * Todo el encabezado, estilos y estructura viven dentro de <Dashboard />
 * (ver components/Dashboard.tsx y app/globals.css para el sistema de diseño).
 */
export default function HomePage() {
  return <Dashboard />;
}
